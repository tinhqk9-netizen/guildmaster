# 08. Batch 1 Backend Issue Register (Đã Cập Nhật Thực Tế)

Báo cáo danh sách các lỗi cấu trúc dữ liệu, chênh lệch thiết kế và rủi ro logic được xác minh trực tiếp từ mã nguồn backend.

| Mã Lỗi | Luồng Nghiệp Vụ | Vấn Đề Backend Được Phát Hiện | Bằng Chứng Mã Nguồn | Tác Động Người Chơi | Tác Động Giao Diện (UI) | Khả năng Khắc phục phía UI? | Giải Pháp Khuyến Nghị Cho Backend | Mức Độ |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **B1-01** | Save/Load | Thiếu Event notification như `OnSaveStatusChanged` or `OnDataRefreshed`. | `SaveService.cs` không khai báo event/delegate nào báo trạng thái lưu/tải dữ liệu thành công. | Người chơi không biết khi nào game đang tự động lưu dữ liệu. | UI không thể hiển thị Save Spinner (icon lưu game tự động xoay) một cách chính xác. | Không. Chỉ có thể dùng coroutine chạy giả lập thời gian hiển thị icon. | Khai báo các event `public Action OnSaveStarted` và `public Action<bool> OnSaveFinished` trong `SaveService`. | THẤP |
| **B1-02** | Dungeon/Party | Trạng thái Đội hình (Active Party) không được ghi vào file Save. | `CharacterScreen.cs:36` (`_partyIds` là HashSet runtime). Không có trường `PartyIds` hay tương tự trong `SaveData.cs`. | Người chơi mất toàn bộ đội hình đã chọn mỗi khi mở lại game hoặc reload scene. | Màn hình tuyển chọn Party luôn trống rỗng mỗi lần Boot, mặc định fallback về Hero được chọn hiện tại. | Không. UI không có quyền ghi trực tiếp vào disk nếu SaveData schema không hỗ trợ. | Thêm trường `public List<string> ActivePartyHeroIds` vào trong `SaveData` và lưu cùng dữ liệu nhân vật. | TRUNG BÌNH |
| **B1-03** | Quest Rewards | Hệ thống thưởng ép buộc Gems cho Quest đặc biệt, phớt lờ chọn lựa Doctrine ở UI. | `QuestService.cs:183-196`. Nếu `quest.Rarity >= 4`, set `isGems = true` và cộng `data.Gems`. UI Doctrine được chọn bị bỏ qua. | Người chơi chọn tăng điểm phát triển Doctrine nhưng thực tế bị nhận Gems thay thế mà không có cảnh báo. | Giao diện hiển thị `(Selected Reward Doctrine: [DOCTRINE])` ở detail card cho cả Quest hiếm, gây ra sự khó hiểu và hiểu lầm về phần thưởng thực nhận. | Có. UI có thể check Rarity của Quest để ẩn lựa chọn Doctrine khi Quest có rarity >= 4. | Thay đổi API `ClaimReward` để trả về kết quả cụ thể (DTO chứa thông tin loại thưởng) để UI hiển thị popup thông báo chính xác. | TRUNG BÌNH |
| **B1-04** | Inventory/Equip | Phục hồi trang bị không qua slot chỉ định cụ thể của UI. | `CharacterScreen.cs:224-226`. UI tự động thử gọi `Equip` lần lượt cho 3 slots của Equipment Service. | Không thể tuỳ chọn mang trang bị (ví dụ 2 Accessory giống nhau). | UI không điều hướng được Button mang vào slot cụ thể của GUI. | Không. Server API `Equip` yêu cầu truyền đúng slot, UI tự động hóa do thiếu thông tin sự kiện click. | Điều chỉnh UI để truyền cụ thể tham số `EquipmentSlot` được người chơi bấm chọn trên giao diện. | THẤP |
