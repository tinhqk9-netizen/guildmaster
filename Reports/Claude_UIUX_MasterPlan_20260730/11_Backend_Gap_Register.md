# 11 — Backend Gap Register

Categories: BACKEND_MISSING, PRODUCTION_CALLER_MISSING, INVALID_STATE_FLOW, SAVE_RESTORE_GAP, DATA_MAPPING_GAP, TIMER_SCHEDULER_GAP, RETURN_VALUE_IGNORED, MISSING_FAILURE_REASON, UI_BACKEND_CONTRACT_GAP, RUNTIME_VERIFICATION_REQUIRED. Severity: BLOCKER/CRITICAL/HIGH/MEDIUM/LOW. Must fix before UI: YES/NO/CAN_PARALLEL/RUNTIME_CHECK_FIRST.

| Gap ID | System | Backend issue | Current behavior | Expected behavior | Player/UI impact | Root cause | Evidence | Severity | Must fix before UI | Suggested backend work | Confidence |
|---|---|---|---|---|---|---|---|---|---|---|---|
| BG-01 | Offline Progress | Two competing implementations (`GameLoopService.ProcessOfflineCatchup` vs `OfflineProgressService.ApplyOfflineProgress`) using different time-reference fields | Unclear which runs; risk of double-processing or the wrong one firing | Offline summary UI cannot be built without knowing which result object is authoritative | Both registered in ServiceContainer, no evidence of call precedence | GameLoopService.cs:39-66; OfflineProgressService.cs:31-62; ServiceContainer.cs:84-85 | CRITICAL | YES | Consolidate to one implementation and one time-reference field; delete the other | CONFIRMED |
| BG-02 | Quest | No method exists anywhere to create/start a `QuestRuntime`/`QuestSaveData` | `SaveData.Quests` can never be populated on a fresh save via any found path | QuestScreen may be permanently empty for every player | Missing `StartQuest`/`AssignQuest` API on IQuestService | QuestService.cs:27-214 (no such method); IQuestService.cs:6-15 | BLOCKER | YES | Add quest-assignment logic (rotation/unlock rules) and a `StartQuest` API | CONFIRMED |
| BG-03 | Merchant | `MerchantRegularStockItems`/`MerchantSpecialReserve` are never populated by any writer | Buy tab will show empty stock on every save | Merchant Buy flow is non-functional in practice | `RollRegularOffer`/`RollSpecialOffer` produce a roll result but nothing converts/appends it to the stock save lists | MerchantService.cs:27-53, 78-116 (no write to stock lists found) | BLOCKER | YES | Add a stock-refresh scheduler that rolls offers into `MerchantRegularStockItems`/`MerchantSpecialReserve` on a timer | CONFIRMED |
| BG-04 | Promotion/Ascension | `PromotionDefinition` not registered in `DatabaseBuilder._categoryLoaders`; no `promotions.json` file exists | `GetAll<PromotionDefinition>()` always returns empty | Promotion/Ascension screen would be permanently a dead end | Data-registration omission | DatabaseBuilder.cs:26-38; PromotionService.cs | BLOCKER | YES | Add `promotions.json` + register category loader | CONFIRMED |
| BG-05 | Dungeon | Nothing ever writes `DungeonSaveData.State = Completed` | `RequiredClearDungeonId` chain-gate can never pass | All chained/locked dungeons are permanently unreachable | Missing completion-write call in `DungeonService`'s state-5/6 transition or `MoveCorpsesAndAwardExperience` | DungeonService.cs (no Completed write found); StartDungeon gate at lines 57-69 | CRITICAL | YES | Write `Completed` state (and increment ClearCount/BestTimeSeconds) on successful dungeon clear | CONFIRMED |
| BG-06 | Status Effects | `StatusEffectService` constructed in `ServiceContainer` but never injected into `CombatService` or any consumer | Status effects are rolled onto data structures but never applied in combat | Combat outcomes/UI (status icons) would show nothing meaningful even if built | Missing DI wiring | ServiceContainer.cs:70; CombatService.cs (no IStatusEffectService field) | HIGH | RUNTIME_CHECK_FIRST | Inject IStatusEffectService into CombatService and call AddStatusEffect from ApplyDamage/skill resolution | CONFIRMED |
| BG-07 | Skills | `SkillService.CreateSkill` ignores its `definition` parameter; no skill-effect resolution exists; `SkillDefinition.NameKey/DescriptionKey` are C# properties (JsonUtility won't deserialize them) | Skills are inert placeholders | A Skills UI would have nothing real to display | Two compounding bugs: field/property mismatch + no effect system | SkillDefinition.cs:6-13; SkillService.cs:11-17 | HIGH | CAN_PARALLEL (no UI planned for Skills in Phase 1-5) | Fix property→field; design and implement skill-effect resolution | CONFIRMED |
| BG-08 | Quest/Skill/StatusEffect Definitions | `QuestDefinition.TargetProgress/TrueClass`, `SkillDefinition.NameKey/DescriptionKey`, `StatusEffectDefinition.Type/IsNegative/IsSerialized` are C# properties; `JsonUtility` only deserializes public fields | These fields are always default/0/null from the database despite JSON containing real values | Any UI trusting `QuestDefinition.TargetProgress` directly (rather than quest_metadata.json) would show wrong numbers | Same class-authoring bug repeated 3 times; `ItemDefinition`/`AdventurerDefinition`/`EnemyDefinition` were already fixed to use fields (confirmed by direct read of ItemDefinition.cs) but these 3 were missed | QuestDefinition.cs:6-11; SkillDefinition.cs:6-13; StatusEffectDefinition.cs:7-12; contrast ItemDefinition.cs (fields, correct) | HIGH | YES for Quest (QuestScreen must keep using quest_metadata.json, not QuestDefinition, until fixed) | Convert the 3 classes' properties to public fields | CONFIRMED (verified directly against current ItemDefinition.cs source, not merely inherited from an older report) |
| BG-09 | Party | No `PartyService`/`PartyDefinition`/persisted party state anywhere | Party membership is UI-local (`CharacterScreen._partyIds`) and lost on screen reload/app restart | Any dedicated Party screen (`10`) would have nothing to persist to | Never built | CharacterScreen.cs:36,200-216 | CRITICAL | YES (for a real Party screen; current fake toggle should be removed/relabeled meanwhile) | Add `PartyService` + `SaveData.PartySlots` | CONFIRMED |
| BG-10 | Shelter | `ShelterService` does not exist; only dead SaveData fields (`LevelShelter`, `UpgradeShelter`, `LevelShelterAutofeed`) and dead formula methods | Entire system non-functional | No screen possible | Never built | SaveData.cs:199-201; FormulaService.cs:150-178,234-237 | MEDIUM | YES (blocks any Shelter UI entirely) | Design and implement ShelterService from scratch | CONFIRMED |
| BG-11 | Raid | `RaidDefinition` is an empty class; no `RaidService`/`RaidRuntime`/`RaidSaveData` | Data loads but is entirely inert | No screen possible | Never built | RaidDefinition.cs:6-9 | MEDIUM | YES (blocks any Raid UI entirely) | Design and implement Raid system from scratch (needs game-design input, not just engineering) | CONFIRMED |
| BG-12 | Headquarters | No concept found anywhere in Services/Models/Definitions/Save | Unclear if this is meant to be a real system or just "the home screen" | IA proposes a Home/HQ screen using only existing currency data — no backend blocker if treated as a UI-only landing page | n/a | backend_trace §6 | LOW | NO (can build as a pure-UI landing page using existing HUD data) | Clarify design intent via `17_User_Review_Decisions.md` DEC-01 | CONFIRMED |
| BG-13 | Craft/Market timers | `CraftService.ProgressWorkshop` (10s flat) and `MerchantService.ProgressMarket` (20s flat) both ignore `IFormulaService.GetSecondsToCraft`/`GetSecondsToSell`, which exist and factor in level/price/purchase-flags | Craft/sell durations never scale with upgrades players paid for | `LevelWorkshopTime`/`UpgradeWorkshopTime`/`LevelMarketTime`/`UpgradeMarketTime` upgrades (if ever exposed) would have no effect | Hardcoded constants bypass existing formula | CraftService.cs:144 (DEFAULT_CRAFT_DURATION_SECONDS=10); MerchantService.cs:149 (DEFAULT_SELL_TIME_SECONDS=20); FormulaService.cs:252-267 | MEDIUM | CAN_PARALLEL (UI can ship with flat timers; fix improves them later without UI rework) | Call the existing formula methods instead of hardcoded constants | CONFIRMED |
| BG-14 | Inventory | `InventoryService.AddItem` throws `InvalidOperationException("Inventory is full")` when over capacity; most callers pre-check `CanAddItem` but `TavernService.GenerateVisitor` writes a starter weapon straight to `SaveData.Items`, bypassing the capacity check entirely | Possible unhandled exception path, and a capacity-bypass for tavern starter weapons | Could crash a service call chain if capacity is exhausted at recruit time | Direct `data.Items.Add` bypass | InventoryService.cs:86-105; TavernService.cs (starter weapon insert) | MEDIUM | RUNTIME_CHECK_FIRST | Route TavernService's starter-weapon grant through InventoryService.AddItem with a capacity check | CONFIRMED |
| BG-15 | Character Traits | 7 of ~10 rare traits rollable by `TavernService.RollRareTrait` (EMPATHETIC, GIFTED, INTIMIDATING, FOCUSED, DRAGON_BLOOD, CURSED, REACTIVE) have no case in `CharacterService.GetTraitMultiplier` and fall to a 1.0-multiplier default | Trait is cosmetic-only for most rolls | A "trait effect" tooltip cannot honestly describe an effect for these 7 | Incomplete switch statement | CharacterService.cs:270-290; TavernService.cs:98-109 | MEDIUM | CAN_PARALLEL (UI can show trait name without claiming an effect; full fix improves later) | Add the missing 7 trait cases | CONFIRMED |
| BG-16 | Promotion stacking | `PromotionService.Promote` sets `IsAscended=true` on every tier (not just a special ascension tier), stacking with each tier's own `StatMultiplier` on top of the flat legacy ×1.5 `IsAscended` bonus | Potential double-multiplier balance bug | Would materially affect stat displays on Adventurer Detail once BG-04 is fixed | Design ambiguity, not confirmed a "bug" vs intended | PromotionService.cs; CharacterService.cs:178 | LOW (moot until BG-04 fixed) | NO (design decision, not urgent) | Confirm intended stacking behavior with design before shipping Promotion UI | HIGH_CONFIDENCE |
| BG-17 | Doctrine | `DoctrineMaxed` flag is read (`IsMaxed()`) but never written by any code path | A "maxed" badge would never appear even if a doctrine is functionally capped | Doctrine overview screen (`10`) cannot show a maxed state honestly yet | Dead write path | DoctrineService.cs:109 (no writer found) | LOW | CAN_PARALLEL (ship without maxed badge, add later) | Set `DoctrineMaxed=true` when a doctrine reaches its cap | CONFIRMED |
| BG-18 | Pets | `EquipToCharacter` has no exclusivity guard — same pet can be equipped to two characters simultaneously, or a character can receive unlimited pets with no cap check beyond what UI might add | Potential double-counted stat bonuses / unintended multi-pet stacking if not actually by design | Pets screen (`10`) needs to know the real intended cap before shipping an assignment picker | Unconfirmed whether multi-pet-per-character is a deliberate design choice | PetService.cs:112-134 | MEDIUM | RUNTIME_CHECK_FIRST | Confirm intended per-character pet cap and add exclusivity guard if single-pet was intended | HIGH_CONFIDENCE |
| BG-19 | Save/Settings default divergence | `SettingsService.ResetToDefault()` sets Notifications/Cloud toggles to `true`, but `SaveData.CreateDefault()`'s field defaults leave them at C#'s `false` default | A fresh save and an explicitly-reset save disagree on 2 toggle values | Minor QA inconsistency, could confuse players comparing "New Game" vs "Reset" | Two independent default-seeding code paths | SettingsService.cs:76-94; SaveData.cs:237-250,282-294 | LOW | CAN_PARALLEL | Align the two default-seed paths | CONFIRMED |
| BG-20 | Save migration | `SaveService.MigrateSave` is a placeholder that only bumps the version number with no actual field migration logic | Any future save-schema change has no real migration path | Not a UI blocker today (SaveVersion is still 1) but a forward risk once this plan's new fields (PartySlots, ShelterService state, etc.) are added | Explicitly a TODO in source | SaveService.cs:137 | MEDIUM | NO (not blocking current UI work, but flag before Phase 2+ adds new save fields) | Implement real per-version migration steps | CONFIRMED |
| BG-21 | Localization | `localization.json` is a literal empty array; no `LocalizationDefinition` class or translation service exists | Any localized string display is impossible today; `SettingsService.GetLanguage/SetLanguage` only stores a code | Settings' language selector (if built beyond the current absence) would not actually change displayed text | Never built | backend_trace §34 | LOW | NO (English-only placeholder text is acceptable for this plan's scope) | Build a real localization pipeline in a future phase | CONFIRMED |

**Blocking-before-UI summary**: BG-01 (offline summary UI), BG-02 (Quest screen usefulness), BG-03 (Merchant Buy usefulness), BG-04 (Promotion/Ascension screen entirely), BG-05 (Dungeon chain progression), BG-09 (real Party screen), BG-10 (Shelter screen entirely), BG-11 (Raid screen entirely) are the gaps that make a corresponding *new* screen pointless to build until fixed. All other gaps are refinements that do not block shipping the UI phases in `14_Master_UI_Implementation_Plan.md`.
