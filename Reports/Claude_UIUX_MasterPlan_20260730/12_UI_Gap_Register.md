# 12 — UI Gap Register

Priority: P0 (core flow uncompletable / important action has no UI / button bound to wrong backend / important data hidden / no feedback risking resource/state loss), P1 (progression/economy hard or impossible to use), P2 (secondary usability), P3 (polish).

| UI Gap ID | Screen/System | Missing/poor UI | Backend capability hidden | Player impact | Priority | Dependency | Proposed solution | Acceptance criteria | Evidence |
|---|---|---|---|---|---|---|---|---|---|
| UG-01 | Inventory | Equip button field exists but never built/bound | EquipmentService.Equip reachable only via Character screen | Player must switch screens to equip an item they're looking at | P1 | none (pure UI) | Add Btn_Equip to `GuildMasterUnifiedApply.BuildInventory`, wire to same call CharacterScreen uses | Equip reachable in 1 tap from Inventory | InventoryScreen.cs:27; GuildMasterUnifiedApply.cs:273-299 |
| UG-02 | Inventory | Sell button field exists, unbound, and its handler is a placeholder redirect text, not a real action | Real sell only reachable via Merchant screen | Confusing dead-end control | P2 | none | Either remove the button entirely or make it a real one-tap-to-Merchant deep link | No inert controls remain | InventoryScreen.cs:192-198 |
| UG-03 | Inventory | Equipped-state stub always returns false | player can't tell if an inventory item is currently worn | Risk of accidentally selling/losing track of equipped gear | P1 | BG none (pure UI/query fix — cross-reference CharacterRuntime.Weapon/Armor/Accessory) | Implement real cross-check against CharacterService | Equipped items visibly flagged in Inventory list | InventoryScreen.cs:97-101 |
| UG-04 | Craft | Ingredient-availability checklist hardcoded to always show "available" (999) | misleads player about what they can actually craft | Player attempts crafts they can't afford, gets a late failure instead of an accurate up-front check | P0 | none (pure UI/data-wiring fix) | Query real inventory quantities via InventoryService | Checklist accuracy = 100% match with actual inventory | CraftScreen.cs:214-218 |
| UG-05 | Quest | Claim result discarded — success shown unconditionally | player cannot trust claim feedback | Silent failure risk (thinks reward was granted when it wasn't) | P0 | none | Use ClaimReward's actual result | Feedback always reflects real outcome | QuestScreen.cs:146-162 |
| UG-06 | Dungeon | Start dungeon feedback shown unconditionally, not gated on StartDungeon's actual outcome; no confirmation before committing party | Player may believe a dungeon started when it silently failed; party gets locked into a run without warning | Resource/state-risk with no confirmation | P0 | none (pure UI) | Check return/state before showing success; add ConfirmPopup before Start | Success message always reflects true state; confirm step exists | DungeonScreen.cs:263-278 |
| UG-07 | Character | Unequip feedback shown unconditionally regardless of actual result | Misleading feedback | P1 | none | Use the (currently unused) return value or add one to EquipmentService.Unequip | Feedback reflects real outcome | CharacterScreen.cs:232-242 |
| UG-08 | Character | Add/Remove Party is UI-local only, not persisted, but presented as a real action | Player loses party assignment on navigation/restart with no warning | P1 (becomes P0 once real Party screen ships) | BG-09 | Replace with real Party screen once backend exists; until then, label clearly as "this session only" | No UI implies persistence that doesn't exist | CharacterScreen.cs:200-216 |
| UG-09 | Global | No confirmation dialog exists for any currency-spending or risk action (Tavern upgrades, Craft upgrade, Dungeon start, Merchant buy) except Settings Reset | Risk of accidental resource loss | P0 | none (pure UI infra) | Build ConfirmPopup (see `08`, `10`) and route risky actions through it | Every action flagged in `03` as "no confirmation" now has one, gated by existing Settings toggles | PopupScreen.cs (OK-only); 03_Player_Action_And_State_Inventory.md |
| UG-10 | Global | No error-vs-info visual distinction — `ShowError` just prefixes "[Error]" text with no color/icon | Errors easy to miss | P2 | none | Distinct error styling (color+icon) | Visual A/B distinguishable at a glance | UIService.cs:110-113 |
| UG-11 | Global | `UIService.ShowScreen`/`ShowInfo`/`ShowError`/`ShowDeferred` silently no-op with only a Debug.LogWarning when target/dialog is missing | Player sees nothing, no diagnostic | P1 | none | Fallback toast even for internal wiring failures during development/QA builds | No silent UI failures | UIService.cs:19-39,98-125 |
| UG-12 | Global | Card list rebuilt from scratch (`ClearContainer`+rebuild) on every single Refresh() across all 7 list screens | Potential visible flicker/hitch on large lists (607 items, 321 recipes) | P2 | none | Incremental diff or lightweight virtualization for the largest lists (Inventory, Craft-Recipes) | No visible hitch on full-capacity lists | UICardFactory.cs:22-27 |
| UG-13 | Global | `CreateProgressBar` exists but is unused by all 7 screens; every percentage (quest %, timers, HP) is text-only | Harder to scan at a glance, weaker accessibility (no non-color visual for magnitude) | P1 | none | Wire the existing component into Quest/Craft/Merchant/Dungeon/Tavern | Every progress/timer value has a paired bar | UICardFactory.cs:149-195; confirmed unused across screens |
| UG-14 | Global | Timers (Tavern visitor, Craft queue, Market listing) are snapshot text only, no live tick while screen is open | Player must navigate away and back to see updated countdown | P1 | none | Add periodic refresh (Dungeon's existing 0.5s Auto-Battle coroutine is the in-codebase precedent) | Timers visibly count down in real time | TavernScreen.cs:60-68; CraftScreen.cs:260; MerchantScreen listings |
| UG-15 | HUD | Currency (Money/Gems) refreshes only on HUD Show(), goes stale while other screens mutate it | Player sees wrong balance until returning to HUD | P1 | none | Event-driven refresh or short poll | Currency never stale by more than one action | HUDController.cs:47-63 |
| UG-16 | HUD/Nav | HUD nav buttons use raw gray/black colors, visually inconsistent with themed action-bar buttons elsewhere | Visual inconsistency undermines readability/hierarchy | P2 | none | Reskin nav buttons to `UITemporaryTheme` palette | Nav visually matches the rest of the app | UIWiringGenerator.cs:216,221 |
| UG-17 | HUD/Nav | Nav buttons use fixed anchoredPosition y-stacking (750→-300), not a layout group; risky on varied aspect ratios | Potential clipping/overlap on non-reference resolutions | P2 | none | Convert to layout-group-driven bottom nav bar | No overlap across tested aspect ratios | UIWiringGenerator.cs:58-77 |
| UG-18 | Settings | Cloud toggle displayed but has no button/control | Dead-end display-only row | P2 | none | Add the missing toggle button | Every displayed setting is interactive | SettingsScreen.cs:65-75 |
| UG-19 | Settings | Toggle changes not auto-persisted; requires separate manual Save | Risk of losing preference changes if player navigates away without pressing Save | P2 | none | Add "unsaved changes" indicator, or auto-persist on toggle | No silent preference loss | SettingsScreen.cs:98-141 |
| UG-20 | Dungeon | Combat cards (enemies/party) built non-interactive with `onClick:null`, visually resemble clickable buttons | Confusing — looks tappable, isn't | P2 | none | Style non-interactive cards distinctly (no button chrome) | Non-interactive cards are visually distinct from real controls | DungeonScreen.cs:188-197; UICardFactory.cs:70-139 |
| UG-21 | Dungeon | No defeat/flee summary shown to player | Player doesn't understand why party reset / progress was lost | P1 | none | Add a Defeat/Flee result popup | Player always sees a reason after a dungeon run ends badly | DungeonService.cs:239-280 |
| UG-22 | Dungeon | Loot collect silently drops overflow items with no per-item report | Resource loss with no explanation | P0 | none (UI can surface DungeonService.CollectDrops' int return more clearly; full per-item detail needs a backend return-value enhancement — CAN_PARALLEL) | Show "N items lost, inventory full" explicitly | Every lost item is explained, not silent | DungeonService.cs:495-516 |
| UG-23 | Quest | Only 3 of 8 doctrines exposed via cycler | Player can't target 5 of 8 doctrines for quest rewards | P1 | none | Replace cycler with full 8-option picker | All 8 doctrines selectable | QuestScreen.cs:29 |
| UG-24 | Doctrine | No overview screen exists for the 8-doctrine meta-progression system at all | Entire system invisible to player except as an obscure claim-target picker | P1 | none (pure UI, backend DoctrineService already supports GetLevel/GetProgress for all 8) | Build Doctrine overview screen (`10`) | Player can view all 8 doctrine levels/progress | QuestScreen.cs:29; DoctrineService.cs |
| UG-25 | Pets | Zero UI despite complete backend | Entire system invisible to player | P1 | none (pure UI — backend ready) | Build Pets screen (`10`) — this is the single best "ready-to-build-now" opportunity in the whole gap register | Pets screen ships in Phase 4 with no backend blockers | PetService.cs (complete); zero UI files reference it |
| UG-26 | Party | No real Party screen | Cannot manage a persistent party | P1 | BG-09 | Build once PartyService exists | Party screen functional post-backend-fix | CharacterScreen.cs:36 |
| UG-27 | Offline Summary | No popup exists at all | Player has no idea what happened while away, despite the backend computing it | P1 | BG-01 | Build once offline-progress ambiguity is resolved | Offline summary shown once per session when meaningful | GameLoopService.cs; OfflineProgressService.cs |
| UG-28 | Global | No `LoadingScreen`/`MainMenuScreen` despite `UIScreenId` enum declaring both values | Dead enum values suggest incomplete boot flow; no loading feedback during DB build | P2 | none | Either remove the unused enum values or build the screens if a real boot/menu flow is planned | UIScreenId enum matches actual screen set 1:1 | UIScreenId.cs |
| UG-29 | Global | No visible confirm/selected-state contrast verified for cards (RUNTIME_VERIFICATION_REQUIRED) | Player may not be able to tell what's selected | P2 | none | Verify and add explicit selection visual if absent | Selected card always visually distinct | RUNTIME_VERIFICATION_REQUIRED |
| UG-30 | Global | No Android hardware back-button handling found wired to `UIService.Back()` | Android back button may exit app instead of navigating back | P1 | none (RUNTIME_VERIFICATION_REQUIRED whether truly absent project-wide) | Wire `Input.GetKeyDown(KeyCode.Escape)` to `UIService.Back()` | Hardware back matches in-UI Back button behavior | RUNTIME_VERIFICATION_REQUIRED |
| UG-31 | Merchant | Buy tab will show empty stock by default (see BG-03) with no distinct "nothing today" empty state currently verified | Screen may look broken rather than intentionally empty | P1 | BG-03 | Add explicit empty-stock messaging regardless of backend fix timing | Empty stock always looks intentional, never broken | MerchantService.cs (stock lists) |
| UG-32 | Quest | Screen may be permanently empty (see BG-02) with no distinct messaging today (RUNTIME_VERIFICATION_REQUIRED for current empty-list appearance) | Screen may look broken | P1 | BG-02 | Add explicit "No active quests yet" empty state regardless of backend fix timing | Never looks broken/blank without explanation | QuestService.cs (no creation path) |

Full raw evidence: `evidence/ui_trace_raw.md`, `evidence/backend_trace_raw.md`.
